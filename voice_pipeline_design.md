# 语音通话管线设计讨论（暂存稿）

## 当前管线图

```
[你] 🗣️ 说话
  │
  ▼
[浏览器 Edge] 录音 → 生成 webm/wav
  │  POST /api/asr (音频二进制)
  ▼
┌─ voice_server.py (bt2:12054) ──────────────────────────┐
│  接收音频 → 检测格式 → ffmpeg 转 16kHz wav              │
│  → noisereduce 降噪                                     │
│  → POST /v1/audio/transcriptions (A3B)                  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─ A3B llama.cpp ────────────────────────────────────────┐
│  (bt2:12026, 同一台机器同一个进程组)                   │
│  whisper 模型 → 输出文字                                │
└──────────────────┬──────────────────────────────────────┘
                   │  返回 {"text":"..."}
                   ▼
┌─ voice_server.py ──────────────────────────────────────┐
│  收到文字 → 同步到聊天记录                               │
│  → POST /v1/chat/completions (Hermes Gateway)           │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─ Hermes Gateway ───────────────────────────────────────┐
│  (127.0.0.1:8642, 跟 voice_server 同机)                │
│  → 转发到 DeepSeek API 云端                             │
│  → 返回完整回答                                         │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─ voice_server.py ──────────────────────────────────────┐
│  收到回答 → 显示在页面 → POST /api/tts                  │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─ TTS (Qwen/Edge) ─────────────────────────────────────┐
│  (192.168.1.5:1234) → 你的 Win10 上的服务              │
│  → 返回 wav 音频                                        │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
[浏览器] 播放音频 🔊 ← 你听到
```

## 关键发现

1. **ASR（A3B）和 Hermes Gateway 都在 bt2 同一台机器上**，只是端口不同（12026 和 8642）。它们之间没有物理网络跳转，只有 localhost 的 IPC。

2. **真正的网络跳转只有两次**：
   - 你的浏览器 → bt2 服务器（ASR + Hermes）
   - bt2 服务器 → 你的 Win10（TTS 服务）

3. **TTS 在 Win10 上（192.168.1.5）**，这是唯一一个跨机器的服务。

## 圆圈设计的讨论

### 当前三圈方案（麦克风 + A3B + Hermes + TTS）
- ASR 和 Hermes 都在 bt2 上，分成两个圈不反映实际网络拓扑
- 用 ping 延迟判断服务是否可用不准确（机器活着但服务挂了，ping 仍然通）

### 建议方案

**三阶段圆圈：🎤录音 → 🧠理解 → 🔊发声**

| 阶段 | 服务 | 位置 | 检测方式 |
|---|---|---|---|
| 🎤 录音 | 浏览器 → voice_server | 你的电脑 → bt2 | 端口检测 bt2:12054 |
| 🧠 理解 | ASR(A3B 12026) + Hermes Gateway(8642) | bt2 本地 | 端口检测 localhost:12026, localhost:8642 |
| 🔊 发声 | TTS 服务 (Qwen/Edge) | bt2 → 你的 Win10 | 端口检测 192.168.1.5:1234 |

**检测逻辑改为端口探测（TCP connect）而非 ping**，更准确：
- 端口通 = 服务在线，显示绿/黄（延迟）
- 端口不通 = 服务离线，显示红叉
