# Voice Gateway — 语音网关集成方案

## 一、项目背景

我们已有一个运行中的 Hermes Agent 语音对话系统，包含：
- **服务端** (voice_server.py)：aiohttp 服务，提供 ASR/TTS/Chat API
- **Web 客户端** (voice.html)：浏览器录音对话
- **桌面客户端** (voice_client.py)：Python tkinter 录音对话
- **网络管理** (network.html)：多设备多网络优先级配置

当前痛点：
1. **降噪不足**：办公室环境噪音影响 ASR 识别率
2. **打断不稳定**：停止按钮在部分浏览器上无效，缺少真正的语音打断检测
3. **实时性不够**：录音→ASR→Chat→TTS 全流程约 3-5s，不够流畅

## 二、目标

将 Rhasspy（或 webrtc-audio-processing + silero-vad）集成到当前架构中，实现：
- 实时降噪（办公室环境可用）
- 可靠的语音活动检测（VAD）和打断
- 端到端延迟 < 2s
- 不改变现有 ASR（A3B llama.cpp）/ TTS（Qwen/Edge）/ Chat（Hermes DeepSeek）管线

## 三、选型分析

### 方案 A：Rhasspy（推荐）
- **类型**：完整的语音网关框架
- **核心能力**：模块化 ASR/TTS，内置 WebRTC 降噪，VAD 打断
- **集成方式**：Rhasspy 做音频前端 → 我们自己的 ASR/TTS/Chat 管线
- **优点**：开箱即用，MQTT/HTTP 接口，离线运行
- **缺点**：需要学习 Rhasspy 配置体系，项目活跃度一般
- **评分**：8.2/10

### 方案 B：webrtc-audio-processing + silero-vad（快速落地）
- **类型**：两个开源库的组合
- **核心能力**：WebRTC 工业级降噪 + silero 高精度 VAD
- **集成方式**：直接在 voice_server.py 的 ASR 接口中加入预处理
- **优点**：今天就能改完，不改架构，零学习成本
- **缺点**：没有完整的打断管理框架
- **评分**：7.5/10

## 四、推荐方案详解（方案 A：Rhasspy）

### 4.1 架构图

```
用户语音输入
    │
    ▼
┌─────────────────────────────────────────────┐
│              Rhasspy 语音网关                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │ WebRTC   │→│ Silero   │→│ 音频    │   │
│  │ 降噪     │  │ VAD      │  │ 路由    │   │
│  └──────────┘  └──────────┘  └──────────┘   │
└─────────────────────┬───────────────────────┘
                      │ (处理后音频流)
                      ▼
┌─────────────────────────────────────────────┐
│          现有 Hermes 管线                     │
│  POST /api/asr (A3B llama.cpp)              │
│       → POST /api/chat (DeepSeek)           │
│       → POST /api/tts (Qwen/Edge TTS)       │
└─────────────────────┬───────────────────────┘
                      │ (音频回复)
                      ▼
┌─────────────────────────────────────────────┐
│            Rhasspy 音频输出                   │
│  ┌──────────┐                               │
│  │ 播放 +   │                               │
│  │ 打断检测 │                               │
│  └──────────┘                               │
└─────────────────────────────────────────────┘
```

### 4.2 Rhasspy 配置要点

```yaml
# Rhasspy 核心配置
语音转文字:
  提供者: external  # 使用外部 ASR（我们的 /api/asr）
  外部ASR地址: http://localhost:12054/api/asr

文字转语音:
  提供者: external  # 使用外部 TTS（我们的 /api/tts）
  外部TTS地址: http://localhost:12054/api/tts

音频处理:
  降噪: webrtc     # 内置 WebRTC 降噪
  采样率: 16000
  声道: 1

语音活动检测:
  提供者: webrtc   # 内置 VAD
  打断模式: 立即   # 检测到用户说话立即打断 TTS 播放
  灵敏度: 3        # 0-3，数字越大越敏感

对话管理:
  唤醒词: 禁用     # 不需要唤醒词
  超时: 5秒       # 无语音超时
```

### 4.3 集成步骤

1. 安装 Rhasspy
2. 配置 Rhasspy 使用外部 ASR/TTS
3. 修改 Rhasspy 音频流对接我们的 voice_server.py
4. 测试降噪效果
5. 测试 VAD 打断
6. 集成到现有 Web 和桌面客户端

### 4.4 预期效果

| 指标 | 当前 | 集成后 |
|---|---|---|
| 降噪 | noisereduce（轻量） | WebRTC 工业级降噪 |
| 打断 | 手动停止按钮 | 语音自动打断 |
| 端到端延迟 | 3-5s | <2s |
| 办公室可用性 | 吵时识别率低 | 正常对话 |

## 五、快速落地备选（方案 B）

如果不引入 Rhasspy，直接在现有代码中增强：

### 5.1 修改 voice_server.py

```python
# 在 /api/asr 接口中，ffmpeg 转码后加入：

import webrtcvad
vad = webrtcvad.Vad(3)  # 灵敏度 0-3

# 对音频做 VAD 检测，只保留有语音的片段
# 过滤掉静音段，减少无效 ASR 请求

# 同时用 noisereduce 做降噪（已实现）
```

### 5.2 前端增强

```javascript
// 在浏览器端用 AudioContext + AnalyserNode 做实时音量检测
// 检测到用户说话时立即自动停止 TTS 播放
```

### 5.3 优缺点对比

| 维度 | 方案 A (Rhasspy) | 方案 B (增强现有) |
|---|---|---|
| 开发时间 | 1-2天配置 | 半天改代码 |
| 降噪效果 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ |
| 打断效果 | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| 维护成本 | 需学 Rhasspy | 无新增依赖 |
| 灵活性 | 受限于 Rhasspy | 自由修改 |
| 推荐度 | **首选** | **快速备选** |

## 六、建议实施路线

1. **第一阶段（半天）**：方案 B 快速落地，验证降噪+VAD 效果
2. **第二阶段（1-2天）**：评估效果，如果不够好就上 Rhasspy
3. **第三阶段（持续）**：根据豆包/社区反馈优化
